import "bootstrap";

import { flipCard } from '../components/flip_card';

document.addEventListener('DOMContentLoaded', (e) => {
  flipCard();
});

import { flipCircle } from '../components/flip_circle';

document.addEventListener('DOMContentLoaded', (e) => {
  flipCircle();
});
